# Notebooks

Abra com o kernel `.venv` criado na raiz de `case/` (`pip install -r requirements.txt`).

| Arquivo | Conteúdo |
|---|---|
| `01_eda.ipynb` | Auditoria: desconto, ciclo, cadastro |
| `02_resultados.ipynb` | ATT, event study, ROI, gates |
| `case_paths.py` | Localiza a raiz do case (não editar) |
